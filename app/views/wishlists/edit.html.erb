<div class="bg-gray-200 h-screen flex items-center justify-center flex-col">
  <div class="w-5/6 mx-auto py-3">
    <div class="flex items-center mb-5">
      <h1 class="text-2xl font-extrbold">Edit your wishlist <%= @wishlist.title %></h1>
    </div>

    <%= form_with model: @wishlist, method: :patch do |f| %>
      <div class="field mb-4 w-64">
        <%= f.label :title, class: "form-label" %>
        <%= f.text_field :title, class: "form-input" %>
      </div>

      <div class="actions">
        <%= f.submit "Update title", class: "form-submit" %>
      </div>
    <% end %>

    <% if @wishlist.wishlist_items.any? %>
      <ul class="font-mono mb-5 pl-5">
        <% @wishlist.wishlist_items.each do |item| %>
          <li>
            <%= item.name %>
          </li>
        <% end %>
      </ul>
    <% end %>

    <%= form_with model: [@wishlist, WishlistItem.new] do |f| %>
      <div class="field mb-4 w-64">
        <%= f.label :name, class: "form-label" %>
        <%= f.text_field :name, class: "form-input" %>
      </div>

      <div class="actions">
        <%= f.submit "Add new wishlist item", class: "form-submit" %>
      </div>
    <% end %>
  </div>
</div>